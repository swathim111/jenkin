pipeline {
agent any
stages {
stage('build')
{
steps {
sh 'printenv'
sh 'docker build -t docker .'
}
}
stage('ecrlogin')
{
steps {
withEnv (["AWS_ACCESS_KEY_ID-${env.AWS_ACCESS_KEY_ID},"AWS_SECRET_ACCESS_KEY-${env.AWS_SECRET_ACCESS_KEY},"AWS_DEFAULT_REGION-${env.AWS_DEFAULT_REGION}"])
{
sh 'docker login -u AWS -p $(aws ecr-public get-login-password --region us-east-1) public.ecr.aws/o4p0t3e5'
sh 'docker tag docker:latest public.ecr.aws/o4p0t3e5/docker:latest'
sh 'docker push public.ecr.aws/o4p0t3e5/docker:latest'
}
}
}
}
}

